<?php

// use App\Http\Controllers\GoogleCalendarWebhookController; // Google Calendar sync disabled
use Illuminate\Support\Facades\Route;

// Google Calendar sync disabled — re-enable this webhook when the feature is needed.
// Route::post('webhooks/google-calendar', [GoogleCalendarWebhookController::class, 'handle']);
